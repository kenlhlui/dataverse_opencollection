info:
    name: Uploads a logo for a dataset
    type: http
    seq: 1
    tags:
        - uploadDatasetLogo
http:
    method: POST
    url: '{{baseUrl}}/datasets/:id/thumbnail'
    params:
        - name: id
          value: '{{id}}'
          type: path
    headers:
        - name: Content-Type
          value: multipart/form-data
    body:
        type: multipart-form
    auth: inherit
docs: Uploads a logo for a dataset
