info:
    name: Admin Check Authentication Provider Enabled
    type: http
    seq: 45
http:
    method: GET
    url: '{{baseUrl}}/admin/authenticationProviders/:id/enabled'
    params:
        - name: id
          value: '{{id}}'
          type: path
    auth: inherit
