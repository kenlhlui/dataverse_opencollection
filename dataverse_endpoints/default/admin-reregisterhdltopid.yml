info:
    name: Admin Reregister Hdl To Pid
    type: http
    seq: 195
http:
    method: POST
    url: '{{baseUrl}}/admin/:id/reregisterHDLToPID'
    params:
        - name: id
          value: '{{id}}'
          type: path
    auth: inherit
