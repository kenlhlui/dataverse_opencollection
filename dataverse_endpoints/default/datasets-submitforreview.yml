info:
    name: Datasets Submit For Review
    type: http
    seq: 298
http:
    method: POST
    url: '{{baseUrl}}/datasets/:id/submitForReview'
    params:
        - name: id
          value: '{{id}}'
          type: path
    auth: inherit
