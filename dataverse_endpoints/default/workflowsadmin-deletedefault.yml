info:
    name: Workflows Admin Delete Default
    type: http
    seq: 188
http:
    method: DELETE
    url: '{{baseUrl}}/admin/workflows/default/:triggerType'
    params:
        - name: triggerType
          value: '{{triggerType}}'
          type: path
    auth: inherit
