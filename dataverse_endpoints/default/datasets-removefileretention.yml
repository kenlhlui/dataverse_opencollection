info:
    name: Datasets Remove File Retention
    type: http
    seq: 271
http:
    method: POST
    url: '{{baseUrl}}/datasets/:id/files/actions/:unset-retention'
    params:
        - name: id
          value: '{{id}}'
          type: path
    auth: inherit
