info:
    name: Pids Reserve Pid
    type: http
    seq: 511
http:
    method: POST
    url: '{{baseUrl}}/pids/:id/reserve'
    params:
        - name: id
          value: '{{id}}'
          type: path
    auth: inherit
