info:
    name: Groups Get Mail Domain Group
    type: http
    seq: 99
http:
    method: GET
    url: '{{baseUrl}}/admin/groups/domain/:groupAlias'
    params:
        - name: groupAlias
          value: '{{groupAlias}}'
          type: path
    auth: inherit
