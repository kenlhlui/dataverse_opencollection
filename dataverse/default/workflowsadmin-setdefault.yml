info:
    name: Workflows Admin Set Default
    type: http
    seq: 187
http:
    method: PUT
    url: '{{baseUrl}}/admin/workflows/default/:triggerType'
    params:
        - name: triggerType
          value: '{{triggerType}}'
          type: path
    auth: inherit
