info:
    name: Replace a set of files to a dataset
    type: http
    seq: 1
    tags:
        - replaceFilesInDataset
http:
    method: POST
    url: '{{baseUrl}}/datasets/:id/replaceFiles'
    params:
        - name: id
          value: '{{id}}'
          type: path
    headers:
        - name: Content-Type
          value: multipart/form-data
    body:
        type: multipart-form
    auth: inherit
docs: Replace a set of files to a dataset
